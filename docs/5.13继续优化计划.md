# 5.13 继续优化计划

## 目标

基于最新 GSC 数据，在已经完成多语言核心页、英文高意图页、sitemap、robots、footer 和语言切换优化之后，下一阶段不继续盲目新增页面，也不立即扩展新语种。

当前重点是提升已有搜索曝光页面的点击率和搜索结果匹配度，尤其是 `/codes/` 和 `/value-list/`。

---

## 当前已完成事项

### 多语言核心页

已完成英文、繁中、泰语核心页：

```text
/
/codes/
/tier-list/
/updates/
/beginners-guide/
/units/

/zh-tw/
/zh-tw/codes/
/zh-tw/tier-list/
/zh-tw/updates/
/zh-tw/beginners-guide/
/zh-tw/units/

/th/
/th/codes/
/th/tier-list/
/th/updates/
/th/beginners-guide/
/th/units/
```

### 英文高意图页

已完成并请求 GSC 索引：

```text
/scripts/
/value-list/
/macros/
```

### 第二优先级页面

已完成：

```text
/discord/
/units/ searched units 模块
```

`/units/` 已加入以下 searched units：

```text
Railgunner
Farm
King
Juggernaut
```

### SEO 基础结构

已完成：

```text
http / https / www 301 规范化确认
sitemap 白名单
robots 对齐 sitemap
canonical / hreflang 对齐
语言下拉同页面切换
footer 收缩并恢复英文高意图入口
未完成多语言旧入口 404
```

---

## 最新 GSC 数据判断

### 1. Script 簇

数据表现：

```text
约 14 点击 / 482 展示 / CTR 约 2.90%
```

代表查询：

```text
noob tower defense script
script noob tower defense
roblox noob tower defense script
noob tower defense scripts
```

判断：

```text
/scripts/ 已经完成
首页入口已强化
footer 英文入口已加入
GSC 已请求索引
```

动作：

```text
暂不继续改，观察 3 到 7 天，看是否从首页分流到 /scripts/
```

---

### 2. Value 簇

数据表现：

```text
约 1 点击 / 423 展示 / CTR 约 0.24%
```

代表查询：

```text
noob tower defense value
value list noob tower defense
noob tower defense value list
value noob tower defense
roblox noob tower defense value list
roblox noob tower defense value
```

判断：

```text
展示量高
点击率极低
排名约在 8 到 11
页面有机会，但搜索结果吸引力和首屏承诺需要继续优化
```

动作：

```text
精修 /value-list/ 的 title、description、hero lede 和首屏表达
```

---

### 3. Codes 簇

数据表现：

```text
约 0 点击 / 128 展示 / CTR 0%
```

代表查询：

```text
noob tower defense codes
noob tower defense code
code noob tower defense
codes for noob tower defense
noob tower defense codes 2026
codes noob tower defense
[season 2] noob tower defense codes
ntd codes
```

判断：

```text
/codes/ 已存在
但 codes 查询没有点击
这不是缺页面，而是搜索结果吸引力或搜索意图匹配存在问题
```

风险：

```text
如果 title 太保守，用户不点
如果 title 过度承诺，比如 100% verified，但页面实际强调 0 verified codes，会造成预期冲突
```

动作：

```text
优化 /codes/ 的 title、description、hero lede 和首屏 summary card
保留 H1，不改 H1
```

---

### 4. Macro 簇

数据表现：

```text
0 点击 / 36 展示 / 平均排名约 7.03
```

判断：

```text
排名不错
展示量还小
/macros/ 刚完成
```

动作：

```text
暂不改，观察 3 到 7 天
如果仍然 0 点击，再考虑把 title 调整为更靠近 auto farm / farming intent
```

候选 title：

```text
Noob Tower Defense Macro Guide 2026 | Auto Farm Safety & Risks
```

---

### 5. Discord 簇

数据表现：

```text
约 0 点击 / 41 展示 / CTR 0%
```

判断：

```text
/discord/ 刚完成
首页入口已加入
footer 英文入口已加入
sitemap 与 robots 已加入
```

动作：

```text
暂不改，观察 3 到 7 天
如果仍无点击，再把 title 改得更贴近 discord server 查询意图
```

候选 title：

```text
Noob Tower Defense Discord Server 2026 | Official Links & Safety
```

---

### 6. Units / Railgunner 簇

数据表现：

```text
约 1 点击 / 29 展示 / CTR 约 3.45%
```

代表查询：

```text
railgunner noob tower defense
noob tower defense railgunner
```

判断：

```text
有搜索信号
但样本太小
/units/ 已增加 searched units 模块
```

动作：

```text
暂不拆 /units/railgunner/
等 Railgunner 相关展示超过 30 到 50 后，再判断是否做独立单位详情页
```

---

## 设备维度提醒

当前数据大致显示：

```text
移动端 CTR 高于桌面
移动端排名略差于桌面
```

判断：

```text
移动端标题和首屏体验非常重要
不要只看桌面页面效果
```

需要检查的页面：

```text
/codes/
/value-list/
/scripts/
```

检查点：

```text
首屏是否能看到核心承诺
按钮是否靠前
标题是否太长
Popular searches 是否过低
广告是否遮挡主要内容
```

---

## 国家维度提醒

当前语言扩展判断：

```text
繁中已完成，台湾 CTR 信号较好
泰语已完成，需要继续观察
印尼仍是下一个语言候选
```

但现阶段不马上做印尼语。

原因：

```text
英文高意图页刚上线
/codes/ 和 /value-list/ 仍有高展示低点击问题
继续扩语言会让判断变混乱
```

---

## 下一阶段执行顺序

### P0：优化 `/codes/`

目标：

```text
提高 codes 查询簇的 CTR
```

当前问题：

```text
codes 查询有展示但没有点击
```

建议修改：

```text
title
description
hero lede
首屏 summary cards
```

不建议修改：

```text
H1
```

推荐 title：

```text
Noob Tower Defense Codes 2026 | Active Status & Community Codes
```

推荐 description：

```text
Check Noob Tower Defense codes, active status, community-reported codes, pending rewards, and safe redeem steps before trusting copied code lists.
```

推荐 lede：

```text
Check active status, community-reported codes, pending rewards, and safe redeem steps before trusting copied code lists.
```

原则：

```text
不要过度承诺 100% verified
不要只强调 0 verified codes
要同时表达 active status、community reported codes、safe redeem guide
```

---

### P1：优化 `/value-list/`

目标：

```text
提高 value 查询簇 CTR
```

当前问题：

```text
value 簇展示高，但点击率极低
```

建议修改：

```text
title
description
hero lede
首屏 summary cards
FAQ
```

推荐 title：

```text
Noob Tower Defense Value List 2026 | Unit Values, Rarity & Trades
```

首屏需要明确出现：

```text
unit values
trade value
rarity
demand
copied value tables
```

原则：

```text
不编造固定价格表
不声称所有 value 已验证
强调 value list 是决策框架，而不是保证价格
```

---

### P2：移动端首屏检查

重点页面：

```text
/codes/
/value-list/
/scripts/
```

检查标准：

```text
用户在手机首屏能不能立刻知道页面能解决什么问题
核心按钮是否在首屏可见
是否被广告或大图压低重点内容
标题是否过长导致折行过多
```

---

### P3：观察新页面 3 到 7 天

观察页面：

```text
/scripts/
/value-list/
/macros/
/discord/
/units/
```

观察指标：

```text
是否被索引
是否有展示
是否开始从首页分流
查询词是否匹配页面主题
CTR 是否比首页更好
```

---

### P4：再决定是否扩印尼语

触发条件：

```text
泰语或繁中页面开始有稳定展示
英文新页面开始被索引
sitemap 22 个 URL 全部正常
/codes/ 和 /value-list/ 的 CTR 有改善或已完成一轮测试
```

如果触发，再执行：

```text
/id/
/id/codes/
```

不建议一次性做完整印尼语 6 页。

---

## 暂不做事项

```text
不新增更多英文页面
不马上扩印尼语
不马上翻译 /scripts/、/value-list/、/macros/、/discord/
不拆 /units/railgunner/
不恢复大量 footer 长尾入口
不把未完成多语言页加入 sitemap
```

---

## 最终判断

当前最优先动作是：

```text
1. 优化 /codes/，因为 codes 簇有展示但 0 点击
2. 精修 /value-list/，因为 value 簇展示高但 CTR 极低
3. 观察 /scripts/、/macros/、/discord/ 的新页面索引和分流效果
```

新增页面和扩语言都不是当前最高优先级。
