// input: Next.js runtime configuration
// output: framework config for App Router build
// pos: project root config

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  trailingSlash: true
};

export default nextConfig;
